import OffCanvas from "../offcanvas";

const TransaksiByDay = () => {
  return (
    <OffCanvas id="TransaksiByDay" title={`Penjualan Tanggal : ${new Date()}`}>
      penjualan
    </OffCanvas>
  );
};

export default TransaksiByDay;
